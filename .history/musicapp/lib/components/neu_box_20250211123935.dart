import 'package:flutter/material.dart';

class NeuBox extends StatelessWidget {

  final Widget? child;

  const NeuBox({super.key,requi});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}